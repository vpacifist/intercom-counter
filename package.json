{
  "name": "intercom-counter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --test && node --check src/content/intercom-observer.js && node --check src/popup/popup.js",
    "validate": "web-ext lint --ignore-files store-assets store-assets/** output output/**",
    "build": "web-ext build --overwrite-dest --ignore-files store-assets store-assets/** output output/**"
  },
  "devDependencies": {
    "web-ext": "^10.1.0"
  }
}
